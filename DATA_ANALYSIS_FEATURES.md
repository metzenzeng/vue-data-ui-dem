# 数据分析功能说明

Workspace 页面现在包含强大的数据分析功能，帮助用户深入了解上传的数据。

## 🎯 核心功能

### 1. 数据质量分析 🔍

自动检查数据质量，包括：
- **数据完整度**：计算非空值占比（颜色编码：绿色≥90%，黄色≥70%，红色<70%）
- **空值统计**：统计每列的空值数量
- **重复行检测**：识别完全重复的数据行
- **问题提示**：自动列出发现的数据质量问题

**使用场景**：
- 上传数据后立即了解数据质量
- 发现需要清理的数据问题
- 评估数据可用性

### 2. 列统计信息 📊

为每一列提供详细的统计信息：

**所有列类型**：
- 列名和数据类型（自动检测）
- 空值数量
- 唯一值数量

**数值列额外统计**：
- 最小值
- 最大值
- 平均值
- 总和

**数据类型自动检测**：
- 🔵 **数字**：自动识别数值列
- 🟣 **文本**：字符串类型
- 🟡 **日期**：日期时间类型
- 🟢 **布尔**：布尔值类型

### 3. 数据可视化 📈

**折线图**：
- 选择 X 轴（任意列）和 Y 轴（数值列）
- 自动生成趋势图
- 适合展示时间序列或连续数据

**柱状图**：
- 对比不同类别的数值
- 适合展示分类数据对比

**使用步骤**：
1. 选择 X 轴列（通常是分类或时间列）
2. 选择 Y 轴列（必须是数值列）
3. 选择图表类型（折线图或柱状图）
4. 图表自动更新

### 4. 分组统计 📊

对数据进行分组聚合分析：

**支持的聚合操作**：
- **求和 (Sum)**：计算每组的总和
- **平均值 (Avg)**：计算每组的平均值
- **计数 (Count)**：统计每组的记录数
- **最大值 (Max)**：找出每组的最大值
- **最小值 (Min)**：找出每组的最小值

**可视化展示**：
- 饼图展示各组占比
- 表格展示详细数值

**使用场景**：
- 按地区统计销售额
- 按产品类型统计数量
- 按部门统计绩效
- 任何需要分组汇总的场景

## 📋 功能建议

### 已实现的功能 ✅

1. ✅ 数据质量检查
2. ✅ 列统计信息
3. ✅ 自动数据类型检测
4. ✅ 数据可视化（折线图、柱状图）
5. ✅ 分组统计（饼图 + 表格）

### 可扩展的功能 💡

#### 1. 数据筛选和搜索
```typescript
// 建议添加：
- 按列筛选数据
- 搜索特定值
- 多条件组合筛选
- 保存筛选条件
```

#### 2. 数据透视表
```typescript
// 建议添加：
- 行维度选择
- 列维度选择
- 值字段聚合
- 动态透视分析
```

#### 3. 数据导出增强
```typescript
// 建议添加：
- 导出筛选后的数据
- 导出统计报告
- 导出图表为图片
- 导出为 Excel 格式
```

#### 4. SQL 查询编辑器
```typescript
// 建议添加：
- BigQuery SQL 编辑器
- 查询历史记录
- 查询结果缓存
- SQL 模板库
```

#### 5. 数据对比
```typescript
// 建议添加：
- 上传多个文件对比
- 列对比分析
- 数据差异检测
- 版本对比
```

#### 6. 数据洞察和建议
```typescript
// 建议添加：
- AI 驱动的数据洞察
- 异常值检测
- 趋势预测
- 数据质量改进建议
```

#### 7. 高级可视化
```typescript
// 建议添加：
- 散点图（相关性分析）
- 热力图（数据密度）
- 箱线图（分布分析）
- 组合图表
```

#### 8. 数据转换
```typescript
// 建议添加：
- 列类型转换
- 数据清洗工具
- 缺失值填充
- 数据标准化
```

## 🚀 使用示例

### 示例 1：销售数据分析

1. **上传销售数据** (`sample-data.csv`)
2. **查看数据质量**：检查完整度和空值
3. **查看列统计**：了解销售额的分布（最小值、最大值、平均值）
4. **创建可视化**：
   - X 轴：日期
   - Y 轴：销售额
   - 图表类型：折线图
5. **分组统计**：
   - 分组列：地区
   - 统计列：销售额
   - 操作：求和
   - 查看各地区销售总额

### 示例 2：用户数据分析

1. **上传用户数据** (`sample-user-data.csv`)
2. **查看数据质量**：检查数据完整性
3. **查看列统计**：了解薪资分布、绩效评分等
4. **创建可视化**：
   - X 轴：部门
   - Y 轴：平均薪资
   - 图表类型：柱状图
5. **分组统计**：
   - 分组列：部门
   - 统计列：绩效评分
   - 操作：平均值
   - 查看各部门平均绩效

## 💡 最佳实践

1. **先检查数据质量**：上传后先查看数据质量分析，了解数据状态
2. **查看列统计**：了解每列的数据特征，选择合适的列进行可视化
3. **选择合适的图表**：
   - 时间序列数据 → 折线图
   - 分类对比 → 柱状图
   - 占比分析 → 分组统计（饼图）
4. **利用分组统计**：快速了解不同维度的数据汇总

## 🔧 技术实现

- **数据分析工具**：`src/utils/dataAnalysis.ts`
- **类型检测**：自动识别数字、文本、日期、布尔值
- **统计计算**：实时计算各种统计指标
- **图表组件**：使用 `vue-data-ui` 组件库

## 📝 注意事项

- 大数据集（>10,000 行）可能影响性能，建议先采样
- 日期格式需要标准化才能正确识别
- 数值列的空值会被自动过滤
- 图表数据会自动限制显示数量（建议 < 100 个数据点）
