# Mock 数据使用说明

本项目已配置 Mock 模式，可以在没有后端服务器的情况下测试上传功能。

## 启用 Mock 模式

Mock 模式在开发环境下默认启用。如果需要手动控制，可以在 `.env` 文件中设置：

```bash
# 启用 Mock 模式
VITE_USE_MOCK=true

# 禁用 Mock 模式（使用真实后端）
VITE_USE_MOCK=false
```

## 示例文件

项目提供了三个示例 CSV 文件，位于 `public/` 目录：

1. **sample-data.csv** - 产品销售数据
   - 包含：产品名称、销售额、数量、日期、地区、客户类型
   - 20 行数据

2. **sample-sales-data.csv** - 月度销售数据
   - 包含：月份、销售额、订单数、平均订单金额、增长率
   - 12 行数据（全年数据）

3. **sample-user-data.csv** - 用户数据
   - 包含：用户ID、用户名、邮箱、部门、入职日期、薪资、绩效评分、状态
   - 15 行数据

## 使用方法

1. **访问 Workspace 页面**
   - 在侧边栏点击 "Workspace" 或访问 `/workspace`

2. **创建工作区**
   - 输入工作区名称（例如：测试工作区）
   - 可选：输入描述
   - 点击"创建工作区"

3. **下载示例文件**
   - 在文件上传区域，点击示例文件链接下载测试数据

4. **上传文件**
   - 拖拽下载的文件到上传区域，或点击"选择文件"
   - 点击"上传文件"按钮
   - 等待处理完成（Mock 模式会模拟 2 秒的处理时间）

5. **查看结果**
   - 查看 BigQuery 表信息
   - 查看数据预览表格
   - 点击"在 BigQuery 中查看"链接（Mock 模式下为示例链接）

## Mock 功能说明

Mock 模式会：

- ✅ 模拟工作区创建（生成随机 ID）
- ✅ 解析 CSV 文件（真实解析）
- ✅ 模拟 Excel 文件处理（返回示例数据）
- ✅ 生成 BigQuery 表信息（Mock 数据）
- ✅ 模拟网络延迟（800ms - 2000ms）

## 测试流程

### 测试 CSV 上传

1. 下载 `sample-data.csv`
2. 创建工作区
3. 上传 CSV 文件
4. 验证数据预览是否正确显示

### 测试 Excel 上传

1. 将 `sample-data.csv` 转换为 Excel 格式（或使用 Excel 创建测试文件）
2. 创建工作区
3. 上传 Excel 文件
4. 验证 Mock 返回的示例数据

### 测试错误处理

1. 尝试上传不支持的文件格式（如 .txt）
2. 验证错误提示是否正确显示

## 切换到真实后端

当后端 API 准备好后：

1. 设置 `.env` 文件：
   ```bash
   VITE_USE_MOCK=false
   VITE_API_BASE_URL=http://your-backend-url/api
   ```

2. 重启开发服务器

3. 所有 API 调用将转发到真实后端

## 注意事项

- Mock 模式下的 BigQuery 链接是示例链接，不会真正打开 BigQuery
- Excel 文件在 Mock 模式下返回的是模拟数据，不是真实解析结果
- 生产环境建议禁用 Mock 模式
